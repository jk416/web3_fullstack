# Web3 全栈项目规划（TypeScript/React 前端 + Go 后端）

> **一句话**：用一个真实的 Web3 项目（钱包 + 迷你交易所 dApp），把 Go 后端通用能力、Web3 链上业务、React 前端，全部融合到同一套代码里，项目驱动、边做边学、每关产出面试素材。
>
> **给谁用**：8 年 Java 后端工程师，转型 Go + Web3 远程后端。已有 Go 基础和一个 gin_gorm 充提币原型，现在推倒重来、按全栈项目重新组织学习。
>
> **怎么用**：这是总纲。开新对话时，把这份文档作为第一条上下文发给 AI，说"按这份规划带我从阶段 0 开始，一次一小关"。

---

## 一、项目是什么

**项目名（暂定）**：`web3-wallet-exchange` —— 一个托管式 Web3 钱包 + 迷你现货交易所 dApp。

**最终形态**（做完长这样）：

```
用户打开 React 写的网页
  → 用浏览器钱包（MetaMask）签名登录（免密码，Web3 经典 SIWE）
  → 看到系统分配的专属充值地址
  → 往地址转 ETH / USDT
  → 前端实时看到"充币到账"（余额自动更新）
  → 查看实时行情（BTC/USDT 等，来自币安）
  → 下单交易（限价单，冻结余额）
  → 发起提币 → 系统签名广播上链 → 前端看到状态流转到"提币成功"
```

**为什么选这个项目**：它是华人 Web3 远程后端岗位最对口的方向（钱包/托管/充提/链上数据），而且天然能承载所有要学的通用能力——不是玩具 demo，是能写进简历、面试能讲 30 分钟的真实系统。

---

## 二、技术栈总览

| 层 | 技术 | 承载的学习目标 |
|----|------|---------------|
| **前端** | TypeScript + React + Vite；Web3 库用 wagmi + viem | React 基础、TS、调用浏览器钱包、前后端联调 |
| **后端** | Go + Gin | Go 进阶、并发、微服务 |
| **链交互** | go-ethereum | 钱包、充提币、事件监听、EIP-1559、签名 |
| **数据库** | PostgreSQL + GORM | SQL 深化、事务、索引、金额精度 |
| **缓存** | Redis | 缓存设计、行情缓存 |
| **消息队列** | Kafka | MQ 解耦、幂等消费、削峰 |
| **服务通信** | gRPC + protobuf | 微服务拆分、行情独立服务 |
| **可观测** | Zap（日志）+ Viper（配置） | 结构化日志、配置管理 |
| **部署** | Docker + docker-compose | 容器化、一键启动 |
| **链** | Sepolia 测试网 + 以太坊主网（只读扫描） | 真实链上交互，不花真钱 |

> **关于前端定位**：前端用 TypeScript + React，这是 Web3 前端的绝对主流（招聘匹配度最高）。Web3 交互用 **wagmi + viem**（当前主流的以太坊 React Hooks 库，替代老的 web3.js/ethers 直接调用），连接钱包、发起签名、读链上数据都很顺手。**注意主次**：后端 Go 才是你的求职主线，前端做到"能演示完整流程、界面能看"即可，不必追求精美 UI。样式用现成组件库（如 Ant Design / shadcn）省时间，别自己手写 CSS 耗精力。

---

## 三、系统架构（文字版，做完你要能默画出来）

```
┌─────────────────────────────────────────────────┐
│  React 前端 (TypeScript + wagmi/viem，跑在浏览器) │
│  登录 / 钱包 / 充值 / 行情 / 下单 / 提币 页面        │
└───────────────┬─────────────────────────────────┘
                │ REST (JSON) + WebSocket
                ▼
┌─────────────────────────────────────────────────┐
│  Go 后端 (Gin)                                    │
│  ├─ handler 层：参数校验、鉴权、调 service          │
│  ├─ service 层：业务逻辑、事务、链交互              │
│  ├─ 后台 goroutine：扫链 / 确认 / 广播 / 行情订阅   │
│  └─ gRPC：行情服务（可拆为独立进程）                │
└───┬──────────┬──────────┬──────────┬─────────────┘
    │          │          │          │
    ▼          ▼          ▼          ▼
┌────────┐┌────────┐┌────────┐┌──────────────┐
│Postgres││ Redis  ││ Kafka  ││ 以太坊节点     │
│账本/订单││行情缓存 ││充币解耦 ││(Sepolia/主网) │
└────────┘└────────┘└────────┘└──────────────┘
```

**数据流两条主线（要能讲清）**：

1. **充币（链 → 系统）**：扫链 goroutine 发现转账 → 发 Kafka → 消费者记 pending → 确认 loop 等 N 个块 → 加余额 → 前端 WebSocket 收到更新
2. **提币（系统 → 链）**：前端提交申请 → 冻结余额 → 后台 worker 用热钱包私钥签名（EIP-1559）→ 广播 → 确认 loop 等达标 → 状态置成功

---

## 四、教学方法论（开新对话时让 AI 严格遵守）

这套方法是从过往有效的学习中提炼的，专门针对"8 年 Java、Go 表达弱、容易知识碎片化"的情况：

1. **一次只推进一个小关卡**，学完立刻在项目里落地，不空讲理论、不一次塞太多。
2. **先讲业务本质**，从真实 Web3/交易所后端视角解释"这个功能解决什么现实问题"。
3. **Java 类比优先**：每引入一个 Go 概念，先问"这对应 Java 的什么"。前端 React/TS 概念，用后端视角类比（组件 ≈ 可复用的 UI 函数、状态 ≈ 内存里的变量、hooks ≈ 生命周期回调）。强调"概念你早会，只是学新语言/框架怎么表达"，避免妄自菲薄。
4. **明确文件/函数位置**：每个任务说清写在哪个文件、哪个函数，不许"随便放"。
5. **先让用户写，再 review**：不主动贴完整实现（简单 struct/DTO/配置样板除外）。给目标、关键 API、边界情况、期望行为，让用户先写。
6. **Review 三段式**：先肯定写对的（具体说为什么对）→ 再指出 bug/生产缺口（说清为什么是问题）→ 最后给下一个小任务。
7. **执行顺序追踪**：review 时追踪变量从赋值到使用的完整链路，不只看孤立行；确认"赋值先于使用"。
8. **每关结束做"串联"**：新学的东西怎么和已有的连起来，防碎片化。感觉乱了就停下结网，不硬加新知识。
9. **每关产出中英对照面试题文档**（放 `docs/interview/`），结构：① 真实公司高频问题 ② 简洁正确参考答案 ③ 你当前代码的差距（兼作重构清单）。能用"业务本质 + 我的实现 + 生产差距"三段式讲清楚才算过关。

---

## 五、求职红线（每关代码都必须满足，面试第一轮就问，写错基本出局）

### 资金安全（最高优先级）
- **金额禁止 `float64`**：用链上最小单位（wei）以 `big.Int` 存，DB 用 `DECIMAL(36,0)`，全程不碰浮点。
- 余额变更一律**原子 SQL**（`UPDATE ... SET balance = balance - ? WHERE balance >= ?`），禁止"读出来→内存改→Save"。
- 涉及钱的多步操作必须在**同一事务**，要么全成功要么全回滚。
- **幂等**：链上 `tx_hash`（ERC20 再加 log index）做唯一键；状态流转用 `RowsAffected` 做乐观抢占。

### 链上交互（当前主网水平，不是 2020 年写法）
- 交易用 **EIP-1559**（`types.DynamicFeeTx` + `GasFeeCap`/`GasTipCap`），不用 `LegacyTx`。
- **nonce 管理**：高并发提币不能每笔 `PendingNonceAt`，要本地串行分配 + 持久化；卡单用"相同 nonce + 更高 gas"重发（replace-by-fee）。
- **reorg 处理**：存区块 hash，检测父块变化时回滚 pending；理解"确认数 ≠ 终局性"。
- 私钥：理解 KMS/HSM/MPC、热冷钱包分层、归集（sweeping）；至少做到不明文进 Git（环境变量），能讲清生产该用 KMS。

### 工程化（每关带着做，不堆到最后）
- `context` 贯穿所有 IO 和后台 loop，支持超时与优雅退出。
- 后台 goroutine 必须 `recover` + 结构化日志，单任务崩溃不拖垮进程。
- 错误用 `errors.Is`/`errors.As` 区分业务错误（400）和系统错误（500）。

---

## 六、英语并行 track（低负荷，嵌进日常）

目标是"工程师英语"不是"流利口语"。优先级：**读（JD/文档）> 异步写（PR/commit/README）> 面试听说 > 日常口语**。

- 代码相关全切英文：commit、注释、PR、README、Issue。写不顺先写中文丢给 AI 翻译 + 讲错因。
- 文档读英文原文：go-ethereum godoc、EIP 规范、wagmi/viem 文档、Kafka 官方文档。
- AI 当外教：你写的英文让它改并解释；出英文面试题你答它纠。
- 技术词表随项目攒：nonce / confirmation / reorg / idempotent / throughput / settlement / consumer group / partition / hook / signer …
- 口语只为面试突击：项目写成 5 分钟英文自述背熟；投递前 AI 语音模拟。
- 兜底：口语弱就先主攻中文技术面的华人远程岗，英语在岗位上边做边补。

---

## 七、分阶段路线图

> 核心设计：**每个阶段都产出同一个项目的一个可运行增量**。每阶段自然融合"若干通用能力 + 若干 Web3 知识 + 对应的 React 前端页面"。做完一个阶段，项目就多一个能演示的完整功能。

### 阶段 0：项目地基（骨架 + 双端环境跑通）

**目标**：一个空但能跑的全栈骨架，前后端能通信，一键启动。

- 后端 Go：项目分层结构（handler/service/model/global/router）、Gin、GORM 连 Postgres、Viper 读配置、Zap 结构化日志、一个 `/health` 接口
- 前端 React：装 Node + pnpm，用 Vite 建 React + TS 项目，Hello World 页面，能 fetch 后端 `/health` 并显示
- 部署：docker-compose 起 Go + Postgres + Redis，前端本地 `pnpm dev`
- **融合的通用能力**：Viper 配置、Zap 日志、Docker（对应旧规划阶段 2、3）
- **前端入门**：TS 类型系统（和 Java 类型对比）、React 组件/props/state/useState/useEffect、Vite 项目结构、fetch/axios 调接口
- **交付**：`docker compose up` 起后端，前端页面能显示后端返回的 health 状态 + 面试题文档

### 阶段 1：用户系统 + Web3 签名登录（SIWE）

**目标**：用钱包签名登录，替代账号密码。这是 Web3 后端高频考点。

- 后端 Go：User 模型、JWT 中间件、EIP-712 / SIWE 签名验证登录接口
- 前端 React：登录页，用 wagmi 的 `useConnect`/`useSignMessage` 连接 MetaMask 并发起签名，拿签名换 JWT
- **融合的通用能力**：interface（鉴权抽象）、错误处理（errors.Is 区分 400/500）、JWT
- **Web3**：EIP-712 结构化签名、SIWE（Sign-In with Ethereum）原理
- **交付**：MetaMask 签名 → 登录成功拿到 JWT → 前端存 token 后续请求带上

### 阶段 2：钱包系统

**目标**：每个用户拿到专属充值地址，私钥安全托管。

- 后端 Go：go-ethereum 生成地址、私钥加密存储（AES，密钥走环境变量）、绑定 userID、同用户不重复生成
- 前端 React：展示用户充值地址 + 二维码
- **融合的通用能力**：值接收者 vs 指针接收者、context、包级变量初始化顺序
- **Web3**：HD 钱包/私钥/公钥/地址推导、私钥安全（热冷钱包分层认知）
- **交付**：登录后前端显示"你的 ETH 充值地址"

### 阶段 3：充币系统（ETH）—— 并发工程化主场

**目标**：往地址转 ETH，系统自动扫链入账，前端实时看到到账。

- 后端 Go：扫链 goroutine（`for + select + ticker`）、context 优雅退出、recover 兜底、scan_checkpoint 断点续扫、Deposit 状态机（pending→confirmed）、tx_hash 幂等（OnConflict）、确认 loop（等 N 个块）、金额全程 `big.Int`
- 前端 React：充值记录列表、余额展示、WebSocket 实时推送"到账"更新
- **融合的通用能力**：goroutine 工程化、context、panic/recover、优雅退出、WaitGroup、金额精度（big.Int/DECIMAL）
- **Web3**：区块扫描、确认数、reorg 认知
- **交付**：Sepolia 往地址转 ETH → 前端几十秒后看到余额增加

### 阶段 4：充币系统（ERC20/USDT）+ Kafka 解耦

**目标**：支持 USDT 充值，并用消息队列把"扫链"和"入账"解耦。

- 后端 Go：`FilterLogs` 监听 USDT 合约 Transfer 事件、解析 Topics/Data、6 位小数换算；Kafka 改造——扫链发消息 → 独立消费者入账
- 前端 React：多币种余额展示（ETH + USDT）
- **融合的通用能力**：Kafka（Topic/Partition/Consumer Group/offset、幂等消费、消息不丢失、削峰）——对应旧规划阶段 4
- **Web3**：ERC20 本质（合约内部账本）、Transfer 事件、tx_hash+logIndex 幂等键
- **交付**：USDT 充值到账 + 扫链与入账通过 Kafka 解耦

### 阶段 5：提币系统 —— 资金安全 + 链上写操作主场

**目标**：用户发起提币，系统用热钱包私钥签名广播上链，全程状态机保证账目一致。

- 后端 Go：提币状态机（pending → processing → broadcast → confirmed / failed）、申请时事务内冻结余额、后台广播 worker（processing 抢占防重复）、EIP-1559 `DynamicFeeTx` 签名广播、确认 loop 解冻、nonce 管理
- 前端 React：提币表单（地址/金额校验）、提币记录列表、状态实时更新
- **融合的通用能力**：事务原子性、错误处理、乐观锁抢占
- **Web3**：交易构建/签名/广播、EIP-1559（baseFee/tip/maxFee）、nonce 管理、replace-by-fee、reorg 安全
- **交付**：Sepolia 真实提币，前端看到状态从申请一路走到"提币成功"

### 阶段 6：行情系统 + gRPC 拆分

**目标**：实时行情展示，并把行情做成独立 gRPC 服务（微服务练习）。

- 后端 Go：币安 WebSocket 订阅、Hub 广播、Redis 缓存最新价、REST 查价接口；用 protobuf 定义行情服务，gRPC 拆成独立进程，主服务通过 gRPC 调
- 前端 React：实时行情页（WebSocket 连后端），价格实时跳动
- **融合的通用能力**：gRPC + protobuf、WebSocket、Redis 缓存、（可选）缓存穿透/雪崩/击穿——对应旧规划阶段 5
- **Web3**：（行情本身非链上，是交易所标配功能）
- **交付**：前端实时显示 BTC/USDT 价格；行情作为独立 gRPC 服务运行

### 阶段 7：订单系统（可选加分，非投递阻塞项）

**目标**：下单/撤单/查询，余额冻结解冻，可选做简单撮合。

- 后端 Go：Order 模型、下单事务冻结（买冻 quote、卖冻 base）、撤单解冻、SymbolMeta 交易对元数据、（可选）内存订单簿 + 价格/时间优先撮合
- 前端 React：下单界面、订单列表、（可选）订单簿深度展示
- **融合的通用能力**：SQL 深化（索引原理、explain、事务隔离级别、行锁、SELECT FOR UPDATE）——对应旧规划阶段 6
- **交付**：下单冻结余额、撤单解冻；撮合视精力而定
- **注意**：撮合引擎是窄众加分项，JD 极少要求。先做下单/撤单/查询，撮合想冲交易所核心岗再补。

### 阶段 8：工程化收尾 + 求职

**目标**：项目达到能自信展示的状态，开始投递。

- 全项目 Docker（Go + React 构建 + PG + Redis + Kafka）一键起
- 多环境配置切换（dev/prod，解决容器 host 与本地 host 矛盾）
- 日志级别可配置（生产关 Debug）
- 基础 CI/CD（GitHub Actions 跑 build + test）
- README（英文）：架构图、技术栈、启动步骤、核心模块、你解决的难点
- 简历：突出 8 年后端 + Java 转 Go + 全栈 Web3 项目
- 投递：电鸭、V2EX 酷工作、电报群、脉脉、LinkedIn；优先中文技术面的华人远程岗

> **投递时机**：不必等 8 个阶段全做完。做到阶段 5（钱包 + ETH/USDT 充币 + 提币）就已具备"最低可展示物"，即可边投边补后面的阶段。用真实面试反推该补什么。

---

## 八、能力覆盖对照表（确认没漏掉任何要学的）

| 要学的能力 | 在哪个阶段落地 |
|-----------|---------------|
| Go interface / 值指针接收者 | 阶段 1、2 |
| goroutine / channel / context / 优雅退出 / recover | 阶段 3 |
| 错误处理（errors.Is/As / %w 包装 / 哨兵错误） | 阶段 1、5 |
| 结构化日志（Zap） | 阶段 0（贯穿全程） |
| 配置管理（Viper / 多环境） | 阶段 0、8 |
| Docker / docker-compose | 阶段 0、8 |
| Kafka（MQ 解耦 / 幂等消费 / 削峰） | 阶段 4 |
| gRPC + protobuf | 阶段 6 |
| SQL（索引 / 事务隔离 / 锁 / 金额精度） | 阶段 2、5、7 |
| Redis（缓存 / 行情） | 阶段 6 |
| 钱包 / 私钥安全 | 阶段 2 |
| 充币（ETH + ERC20）/ 扫链 / 确认 / 幂等 | 阶段 3、4 |
| 提币 / 状态机 / EIP-1559 / nonce / reorg | 阶段 5 |
| SIWE / EIP-712 签名登录 | 阶段 1 |
| 订单 / 撮合（可选） | 阶段 7 |
| TypeScript / React / wagmi / 前端联调 | 阶段 0-7 每阶段配套页面 |
| 英语（读 + 异步写） | 全程嵌入 |

---

## 九、里程碑检查点

| 里程碑 | 验证方式 |
|--------|---------|
| 阶段 0 完成 | docker compose 起后端，React 前端能显示后端返回 |
| 阶段 2 完成 | 钱包签名登录 + 专属充值地址跑通 |
| 阶段 4 完成 | ETH + USDT 充币到账，Kafka 解耦，前端实时更新 |
| 阶段 5 完成（**可投递线**） | Sepolia 真实提币成功，具备最低可展示物 → 开始投 |
| 阶段 6 完成 | 行情实时展示 + gRPC 独立服务 |
| 阶段 8 完成 | 全栈 docker-compose 一键起 + 英文 README + 架构图 |
| 拿到 offer | 目标华人远程后端岗，中文技术面优先 |

---

## 十、如何开启新对话（重要）

开新对话时，第一条消息这样说（照抄即可）：

> "这是我的全栈 Web3 项目学习规划（把这份 `web3 全栈项目规划.md` 全文贴上或用 # 引用）。请严格按第四节的教学方法论带我：一次一小关、先讲业务本质、Java 类比、先让我写你再 review（三段式）、每关产出中英对照面试题文档。现在从阶段 0 开始，先带我把项目地基（Go 骨架 + React 前端 Hello World + docker-compose）搭起来。我是 8 年 Java 后端，Go 有基础但表达弱，前端 React/TS 从零开始。"

**几个提醒**：
- 这个工作区的 `.kiro/steering/mentor.md` 记录了教学风格，同工作区新对话会自动加载。若换新工作区，把本文档第四节的方法论一并贴给 AI。
- 旧的 `gin_gorm` 原型可以作为参考代码保留（很多逻辑能复用），但新项目建议**重开一个干净的仓库结构**，从阶段 0 的骨架长起来，避免背着旧包袱。
- 前端是次要项：后端 Go 才是求职核心。前端做到"能演示流程、界面能看"即可，用组件库省时间，别在 UI 上耗精力。

---

## 十一、心态提醒

> 学不懂先用 AI 弄懂，不死磕超过 2 小时。
> 感觉"知识全是碎片"时，停下来做系统串联，不要继续加新知识。
> 撮合引擎卡住不是你的错——它是窄众加分项，JD 根本不要。
> 不和别人比进度，只和昨天的自己比。
> 项目不完美没关系，能跑通、能讲清楚，就够了。
> 你不是基础差，是 8 年的概念还没接上新语言的表达——接上只是时间问题。

_基于两份旧规划（web3 规划.md + go 通用能力规划.md）融合重排，前端采用主流 TypeScript/React（wagmi + viem），改为单一全栈项目驱动。_
